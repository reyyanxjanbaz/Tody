/**
 * Configuration template — committed to source control.
 * Copy this file to src/config.ts (gitignored) and fill in real values.
 *
 *   cp src/config.example.ts src/config.ts
 */

/** Supabase project URL (Dashboard → Settings → API → Project URL) */
export const SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';

/** Supabase anon (public) key */
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';

/** Render backend base URL */
export const RENDER_BASE_URL = 'https://YOUR_RENDER_APP.onrender.com';
