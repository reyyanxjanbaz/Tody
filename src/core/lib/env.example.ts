/**
 * Supabase client credentials template.
 *
 * Instructions:
 *   1. Copy this file to env.ts (same directory).
 *   2. Fill in your values from Supabase Dashboard → Settings → API.
 *   3. Only the `anon` key goes here — it's safe to commit (RLS protects data).
 *      The `service_role` key must never appear in this repo.
 */
export const SUPABASE_URL      = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY_HERE';
