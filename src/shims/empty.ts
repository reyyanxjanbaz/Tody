// Empty module — aliased in place of native-only side-effect imports
// (e.g. 'react-native-url-polyfill/auto'), which are unnecessary on the web
// where URL / URLSearchParams are built in.
export {};
