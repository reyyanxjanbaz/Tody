/**
 * Build-time feature flags. The Habits tab/route is scaffolded ahead of the
 * Phase 5 habit tracker; flip HABITS_ENABLED to true when that lands so the
 * tab, route, and any habit surfaces light up together.
 */
export const HABITS_ENABLED = true;
